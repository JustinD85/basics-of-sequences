(ns custom-functions.core-test
  (:require [clojure.test :refer :all]
            [custom-functions.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail. FIXED, stop failing.... gosh"
    (is (= 1 1))))
